services:
  app:
    restart: always
    env_file:
      - .env.production
    environment:
      - NODE_ENV=production
      - PORT=3000
      - LOG_DIR=/app/logs
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
